# Quick Capture Palette — Command Palette Recipe Source

This entry preserves the palette visual variant from the Quick Capture
exploration. This is NOT the Quick Capture entry surface direction — see
`2026-04-28-quick-capture-entry-surface` for the product decision.

This entry is adopted as the visual·interaction recipe source for the Cmd+K
Command Palette in Batch 1 (partial adoption — recipe source only, not a full
implementation scope promotion).

## Metadata

- Created: 2026-05-18
- Readiness: code-ready
- Category: visual/interaction recipe source
- Source project: griddo2-claude
- Source topic: Quick Capture prototype exploration
- Source prototype: `origin/prototype/future-ideas`
- Archive status: candidate
- Archive branch: `prototype/future-ideas`
- Archive commit: `e662163`
- Archive route: `/prototype/quick-capture-create-variants` (palette variant)
- Tags: quick-capture, palette, visual-reference, animation

## Summary

The palette prototype from the Quick Capture exploration is adopted as the
visual/interaction recipe source for the Cmd+K Command Palette in Batch 1
(partial adoption — recipe source only, not a full implementation scope
promotion).

The design, animation, and logic are promising. This adoption is scoped to the
Command Palette visual/interaction design only. The Quick Capture entry surface
direction (`surface(main)`) is unchanged.

## Use When

- implementing the Cmd+K Command Palette visual/interaction design (Batch 1)
- revisiting command palette or palette-style interaction
- designing future visual reference/gallery workflows

## Do Not

- treat as Quick Capture entry surface implementation scope — `surface(main)` direction is unchanged
- merge into the `surface(main)` visual direction
- confuse with the Quick Capture entry surface product decision
- treat as a full implementation scope promotion — this is a recipe source only

## Prototype Source

- branch: `prototype/future-ideas`
- route: `/prototype/quick-capture-create-variants` (palette variant)
- commit: `e662163`
